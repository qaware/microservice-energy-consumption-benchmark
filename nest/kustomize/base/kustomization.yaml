apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: nest-config
    literals: []

secretGenerator:
  - name: nest-secrets
    literals: []
