{
  "steps": [
    {
      "id": "step-0-for-{{request.path.itemId}}",
      "name": "name 0 for {{request.path.itemId}}",
      "labels": [
        "{{randomValue length=7 type='ALPHANUMERIC'}}",
        "{{randomValue length=7 type='ALPHANUMERIC'}}"
      ],
      "durationInMs": {{randomInt lower=10 upper=500}}
    },
    {
      "id": "step-1-for-{{request.path.itemId}}",
      "name": "name 1 for item {{request.path.itemId}}",
      "labels": [
        "{{randomValue length=7 type='ALPHANUMERIC'}}"
      ],
      "durationInMs": {{randomInt lower=10 upper=500}}
    },
    {
      "id": "step-2-for-{{request.path.itemId}}",
      "name": "name 2 for item {{request.path.itemId}}",
      "labels": [
        "{{randomValue length=7 type='ALPHANUMERIC'}}",
        "{{randomValue length=7 type='ALPHANUMERIC'}}",
        "{{randomValue length=7 type='ALPHANUMERIC'}}"
      ],
      "durationInMs": {{randomInt lower=10 upper=500}}
    }
  ]
}
