apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-hoverfly
spec:
  replicas: 3
  selector:
    matchLabels:
      name: go-hoverfly
  template:
    metadata:
      labels:
        name: go-hoverfly
    spec:
      containers:
        - name: go-hoverfly
          image: spectolabs/hoverfly:v1.6.3
          args:
            - -webserver
            - -log-level
            - warn
            - -import
            - /simulations/jwks.json
            - -import
            - /simulations/steps.json
            - -response-body-files-path
            - /responses
          volumeMounts:
            - name: go-hoverfly-simulations-volume
              mountPath: /simulations
              readOnly: true
            - name: go-hoverfly-responses-volume
              mountPath: /responses
              readOnly: true
      volumes:
        - name: go-hoverfly-simulations-volume
          configMap:
            name: go-hoverfly-simulations
        - name: go-hoverfly-responses-volume
          configMap:
            name: go-hoverfly-responses
