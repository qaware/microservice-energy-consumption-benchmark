docker_build('rust', '.', only = ['kustomize', 'src', 'Cargo.toml', 'Cargo.lock', 'Rocket.toml'])

k8s_yaml(kustomize('kustomize/environment/local'))

k8s_resource(workload='rust', port_forwards=['8081:8081'])
