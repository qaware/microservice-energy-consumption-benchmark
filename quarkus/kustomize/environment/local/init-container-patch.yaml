apiVersion: apps/v1
kind: Deployment
metadata:
  name: quarkus
spec:
  template:
    spec:
      initContainers:
        - command:
            - flyway
            - migrate
          image: flyway/flyway:10
          imagePullPolicy: IfNotPresent
          name: flyway-migrate
          envFrom:
            - configMapRef:
                name: quarkus-flyway-config
            - secretRef:
                name: quarkus-flyway-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "1000Mi"
            limits:
              memory: "1000Mi"
          volumeMounts:
            - mountPath: /flyway/sql/schema
              name: flyway-migrations-volume
              readOnly: true
      volumes:
        - name: flyway-migrations-volume
          configMap:
            name: quarkus-flyway-migrations
